{
  "name": "comfy-table-wasm",
  "version": "0.1.0",
  "type": "module",
  "main": "pkg/comfy_table_wasm.js",
  "types": "pkg/comfy_table_wasm.d.ts",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "wasm-pack build --target bundler --out-dir pkg",
    "build:web": "wasm-pack build --target web --out-dir pkg-web",
    "test": "wasm-pack test --node",
    "test:js": "node test/test.js"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}